services:
  - type: web
    name: stripe-fastapi-backend
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    envVars:
      - key: STRIPE_SECRET_KEY
        sync: false